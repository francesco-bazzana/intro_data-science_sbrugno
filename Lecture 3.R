#ITERATION

#for() loops, |> pipe operator is the same as %>%

#while() when you do not know the number of oeprations in advance 

#Iteration with purrr --> allows us to do iteration in a consistent way, applyinga function to a object (that can be of any type), and then return an output that is of pre-defined type
#map(.x, .f, ...) 
#map(starwards, class)
#(.x) purr style way of providing a formula ??ss
